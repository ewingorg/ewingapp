ewing = {
			alert : function(title,msg,func){
				mui.alert(msg, title, func);
						
			},
			
			
			/**
			 * 自动消失提示框
			 * @param {Object} msg 信息
			 */
			toast : function(msg){
				mui.toast(msg);
			}
		}
